# O100 build

`gen.py` (with `common.py`) writes the `.docx`: a compliance memo whose byline and last-saved-by are decoys, whose template path names the real author, created at an odd hour, with the giveaway line typed and then deleted as a tracked change. The shipped download is that file, already in this challenge's `files/`.
