# O250 build

`gen.py` (with `common.py`) builds the three files: a week of ADS-B traces with two surveillance flights hidden among the transit fleet, a `sites.csv` of decoy facilities, and a `registry.json` grid. Seeded, so it reproduces the shipped tarball. The download is in this challenge's `files/`.
