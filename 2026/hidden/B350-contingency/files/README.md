# B350 files

This off-board challenge had no downloadable file. It was a page on the CTF domain.

## To stand it up again

- Serve `B350-contingency.html` at an unlisted path on the CTF site (`/contingency`). The page shows a scrambled reconstruction table; ordering it and decoding the hex column yields the recovery key that unlocks the flag.
- Players reach it by following the "contingency" breadcrumb planted across L100, L200, L300, F200 and O300/L400.
- The page will live in this challenge's `build/` folder.
