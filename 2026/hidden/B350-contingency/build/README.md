# B350 build

`B350-contingency.html` is the off-board page. Serve it at an unlisted path on the CTF site (`/contingency`). It shows a scrambled reconstruction table; ordering it and decoding the hex column gives the recovery key that unlocks the flag. Players reach it by following the "contingency" breadcrumb across several challenges.
