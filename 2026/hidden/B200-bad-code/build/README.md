# B200 build

`B200-badcode.html` is the off-board page. Serve it at an unlisted path on the CTF site (`/badcode`). It runs a scripted exchange with "Root" and reveals the flag through a function called in the browser console. Players reach it by following the "signed: bad code" breadcrumb in other challenges.
