# P100 build

`build_p100.py` assembles the three-item dossier: a photo whose EXIF carries part of the designation, an HR PDF with a field hidden under a redaction, and a message export with the reference number. The shipped download is in this challenge's `files/`.
